# CM-001-TD-08 归档 — 修复 error 通知音效静默失败

> 本文件是 CM-001-TD-08 的完整协作记录。当前状态见 `docs/AI_HANDOFF.md`。

## 任务卡摘要

- PHASE: BugFix；PRIORITY: P2
- 目标：修复 `soundManager.playNotificationSound(false)` 访问未配置的 `CONFIG.soundEffects.notifications.error` 导致的静默失败。5 处错误/警告场景（初始化失败、输入校验失败、冷却限制等）传入 `false` 时用户听不到任何反馈。
- 方案（Human 拍板 C）：新增 error 音效文件 + config 配置。音效文件 `sounds/error-notification.wav` 由主 AI 用 Python 生成（200+150Hz 双频蜂鸣，0.35 秒，16-bit PCM mono，30912 bytes）。
- SCOPE：`js/modules/config.js`（加 `notifications.error`）+ `js/modules/sounds.js`（防御性检查）。
- NON-GOALS：不改调用方（`notification.js`）、avatar 音效、`play()` 核心逻辑、`preload()`、全局开关。

## 执行结果（外部 AI 报告要点）

提交（分支 `codex/cm011-error-sound`，基线 `febe3b4`）：

```text
3914a25  fix: 补上缺失的 error 通知音效配置
95d794a  fix: playNotificationSound 增加配置缺失的防御性检查
c9b9d18  docs: 回填 CM-001-TD-08 执行报告与状态
debf202  docs: CM-001-TD-08 验收 PASS，登记合并流程
```

改动：
- `config.js`：`notifications` 加 `error: "sounds/error-notification.wav"` + 用途注释
- `sounds.js`：`playNotificationSound` 加 `if (!url) { console.warn(...); return; }` 防御性检查

## 主 AI 独立验收（2026-09-19）：PASS

7 项验收项全过：

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | config.js 的 notifications 有 success 和 error | ✅ | diff 确认 |
| 2 | sounds.js 防御性检查 | ✅ | diff 确认：`if (!url)` → console.warn + return |
| 3 | playNotificationSound(false) 播放 error 音效 | ✅ | 页面级 14 项断言：play() 收到 error 文件 URL + audioCache 含 error 条目 |
| 4 | playNotificationSound(true) 行为不变 | ✅ | 页面级断言：play() 收到 success 文件 URL |
| 5 | preload() 预加载 error 音效 | ✅ | 页面级断言：audioCache 含 error 条目（Object.values 自动包含） |
| 6 | lint 0 error / git diff --check 0 / check-worktree 缺失 0 | ✅ | 实测 0 problems（比要求更好）/ 0 / 0 |
| 7 | run-all 全量 10/10 不回归 | ✅ | 537 项断言 0 失败；既有 8 套件断言数不变 |

独立复跑：
- 页面级验证 14/0（★ playNotificationSound(false) → play() 收到 error 文件 URL；★ 配置缺失时不调用 play()）
- 反向验证：已改造 14/0 vs 回退 7/7；回退版直接复现缺陷本体（`play(undefined)` → `[null]` 静默失败）；源码已还原 true
- run-all 全量 10/10，537 项断言 0 失败

三处自陈说明裁决：①AC 第 6 项实测 0 problems（比要求更好）✅ ②未新增入库测试套件（SCOPE 只列 2 源文件，AC 未要求，正确判断）✅ ③提交信息重做记录（已复查无残留）✅

## 合并流程

- 快进合并 `codex/cm011-error-sound` → `main`（`febe3b4..debf202`）
- **切 main 时未触发级联** —— 两个分支都有 `scripts/worktree-guard.mjs`，切换不触发 rename → 不级联（GOV-002 修复的附带收益）
- 合并后 lint 0、check-worktree 缺失 0、e2e 29/0 复核通过
- 主 AI 同步 AGENTS.md：CM-001-TD-08 余项标记为已解决

## 流程合规

- 任务卡由主 AI 写入（CURRENT TASK），外部 AI 执行，主 AI 验收 —— 协作流程正确
- 外部 AI 未扩大 SCOPE（未擅自新增入库套件）
- 验收前未合并 main、未 push
