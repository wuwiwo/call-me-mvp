# CM-010 归档 — 访问提示威胁模型、单一来源与诚实文案

> 本文件是 CM-010 的完整协作记录。当前状态见 `docs/AI_HANDOFF.md`。

## 任务卡摘要

- PHASE: Security / Docs / BugFix；PRIORITY: P3
- 目标：给密码门建立书面威胁模型（`docs/SECURITY.md`），让代码、文案、文档三者表述一致且诚实 —— 它是 UI 级访问提示，不是安全边界。同时收敛 `config.js` 与 `password.js` 的密码配置重复定义（单一来源），把用户可见文案改为与实际防护能力相符的诚实表述（四语言）。
- 威胁模型由主 AI 按路线图拍板：资产=无真正受保护资产；对手=任何能打开页面的人；绕过成本≈0；结论=只防误触与不懂技术者，不防有意绕过；`expiryDays=7` 是本地时间戳比较，清 LocalStorage 即重置；不删除功能，不引入 hash/加密/混淆/服务端校验（纯前端无秘密，伪装安全性比明文更有害）。
- SCOPE：`js/modules/password.js`（从 `CONFIG.password` 读，删硬编码）、`js/modules/config.js`（保持唯一来源）、`js/modules/translations.js`（四语言 hint 诚实表述）、`docs/SECURITY.md`（新增）、`tools/password-gate.mjs`（新增，CDP 9450）、`tools/negative-password-gate.mjs`（新增）、`tools/run-all.mjs`（登记新套件）、`tools/README.md`。
- NON-GOALS：不删密码功能；不改触发时机与交互行为；不引入 hash/加密/混淆/服务端校验；不改 LocalStorage key 名与数据格式；不改 webhook/冷却/语言回退；不动 AGENTS.md（主 AI 自己同步）。

## 执行结果（外部 AI 报告要点）

提交（分支 `codex/cm010-password-threat-model`，基线 `ab28b55`）：

```text
6da3f6a  fix: 密码配置收敛到 CONFIG.password 单一来源
1cffdfe  fix: 访问提示文案改为诚实表述（四语言）
f097379  docs: 新增 docs/SECURITY.md 访问提示威胁模型
4676a17  test: 新增 CM-010 访问提示回归与反向验证并登记入口
2f9271b  docs: tools/README 登记 CM-010 工具与端口
bd0d382  docs: 修正统一入口的套件计数（7 → 8）
bb2a9c0  docs: 回填 CM-010 执行报告与状态
```

- `node tools/password-gate.mjs`：60 passed / 0 failed（S0 前置 / S1 触发语义 / S2 验证交互 / S3 模块语义 / S4 诚实文案 / S5 单一来源 / S6 页面异常）
- `node tools/negative-password-gate.mjs`：已改造 60/0 vs 回退 43/17；三类失败标记命中（单一来源 5 条、诚实文案 8 条、损坏时间戳 4 条）；源码已还原 true
- `node tools/run-all.mjs`：10/10 通过，537 项断言 0 失败
- 既有 7 套件断言数 29/51/52/97/87/107/54 与本任务前完全一致

### 三处自陈问题（外部 AI 主动披露）

1. **损坏时间戳行为收紧（偏离括注但符合显式行为要求）**：任务卡要求 3 显式行为是「非法/损坏按『已过期』处理（弹窗）」，括注「与现状 `parseInt` 行为对齐」误述现状 —— 现状是 `NaN > x` 恒 false，坏数据反而永远不要求验证。执行 AI 按显式行为实现并主动披露偏差。
2. **SCOPE 写 `language.js` 实际文案在 `translations.js`**：任务卡笔误，`language.js` 只管语言状态与切换，文案数据在 `translations.js`。执行 AI 改在实际位置。
3. **文档残留 666888 字面量**：`README.md`、`TESTING.md`、`NEW_FEATURES.md`、`docs/DATA_FLOW.md`、`docs/IMPLEMENTATION_CHECKLIST.md`、`.history/` 仍提到，不在 SCOPE 内（SCOPE 仅限 js/），未改。

## 主 AI 独立验收（2026-09-19）：CONDITIONAL PASS

### 9 项验收项逐条核对

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 威胁模型落 `docs/SECURITY.md`，结论与代码/文案一致 | ✅ | 7 节齐备；与 `password.js` 注释、`translations.js` 文案对齐 |
| 2 | `grep 666888 js/` 只命中 `config.js` | ✅ | 实测：`js/modules/config.js:30` 一处；`password.js` 0 命中 |
| 3 | hint 四语言诚实表述，无「安全/保护/加密/授权」暗示 | ✅ | 禁用词扫描四语言 0 命中；均含「绕过/bypass/回避/우회」；四值互不相同 |
| 4 | 损坏时间戳按「已过期」弹窗 | ✅ | `isPasswordExpired` 显式 `Number.isFinite` 校验；反向验证回退版 4 条损坏时间戳断言失败 |
| 5 | 新增 `password-gate.mjs`（60 项，CDP 9450）全绿 | ✅ | 独立复跑 60 passed / 0 failed |
| 6 | 反向验证有区分力 | ✅ | 已改造 60/0 vs 回退 43/17；三类失败标记命中 |
| 7 | 既有 7 套件断言数不变（29/51/52/97/87/107/54） | ✅ | run-all 实测逐项核对一致 |
| 8 | `run-all.mjs` 全量 10/10 通过 | ✅ | 独立复跑 219.1s，537 项断言 0 失败 |
| 9 | lint 0 error / `git diff --check` 0 / check-worktree 缺失 0 / diff 范围受控 | ✅ | eslint 0 error/1 既有 warning（worktree-guard.mjs 未用变量，CM-009 遗留）；check-worktree 缺失 0；diff 9 文件全在 SCOPE |

### 三处自陈问题的裁决

1. **损坏时间戳行为收紧 → PASS**。任务卡括注误述现状；执行 AI 按显式行为实现并主动披露，判断正确。fail-closed 是闸门的合理默认。
2. **SCOPE 文件名笔误 → PASS**。执行 AI 改在实际位置（`translations.js`）并在报告里指出笔误。
3. **文档残留 666888 字面量 → 不在 SCOPE 内 → 记录待后续任务处理**。

### 唯一漏改（主 AI 合并后同步修）

- `js/modules/password.js:97` 的 `password.hint` fallback 字符串仍是旧表述 `'提示：密码每周更新，请联系管理员获取最新密码'`。运行时不触发（四语言翻译键齐全），但与 `docs/SECURITY.md` §6「文案与实现一致性」矛盾；任务卡 SCOPE 写「如确有暗示安全性的措辞一并修正」—— 主 AI 在合并后作为同步提交修这一处（仅一行字符串），不退回执行 AI。

## 合并流程

```text
ab28b55 (CM-009 收尾) → c0ad0a9 (CM-010 任务提交 + 验收登记) → dba7e0c (主 AI 同步)
```

- 快进合并 `codex/cm010-password-threat-model` → `main`（`c0ad0a9`）
- 合并后 `check-worktree` 缺失 0、`password-gate` 60/0 复核通过
- 主 AI 同步提交 `dba7e0c`：
  - 修 `js/modules/password.js:97` fallback 字符串，与 `translations.js` 的 zh.hint 诚实表述对齐
  - 同步 `AGENTS.md`：
    - 「当前数据与安全边界」段补充指向 `docs/SECURITY.md` + 单一来源 `CONFIG.password` 说明
    - 「已知高风险区域」段把密码配置重复定义标记为已收敛，只保留音效配置 error 资源余项（CM-001-TD-08 余项）
- 同步后 `password-gate` 60/0、eslint 0 error、check-worktree 缺失 0 再次复核通过

## 合并时级联事故（第五次实战复发，已恢复）

`git checkout main` 瞬间工作区再次被沙箱级联搬走：post-checkout 钩子报
`MODULE_NOT_FOUND tools/worktree-guard.mjs`（守卫与被保护物同归于尽，同 CM-007/008/009 根因），
`git status` 显示 41 条 ` D`（整个 `docs/` 22 个 + `tools/` 19 个，含 `check-worktree.mjs` 和
`worktree-guard.mjs` 自身）。按既定程序恢复：

```text
git status          # 41 条 D，无非 D 条目 → 满足 restore 前提
git restore -- .    # 只写不删
check-worktree      # 缺失 0
```

零丢失。这是同一根因的第 5 次复发（CM-007/008/009 + GOV-002 实施时现场复发 1 次 + 本次合并时复发 1 次）。GOV-002 守卫修复分支 `codex/gov002-guard-outside-worktree` 已实施 + 自验证，待主 AI 独立验收 + 合并后根治。

## 流程合规

- 协议顺序正确：CM-010 验收完成后才合并 main；未发生「合并先于验收」偏差（CM-008 教训已吸收）。
- 未 push 前所有提交都在任务分支上；合并后主 AI 同步提交在 main 上；push 待 Human 授权。
- diff 范围受控：9 文件全在 SCOPE（password.js/config.js/translations.js/SECURITY.md/password-gate.mjs/negative-password-gate.mjs/run-all.mjs/README.md/AI_HANDOFF.md）+ 主 AI 同步 2 文件（password.js/AGENTS.md）。
