# CM-005 — 消除动态用户输入 HTML 注入

状态：第二轮返工完成，等待主 AI 独立验收；尚未合并 `main`。

## 当前结果

- `buttonManager.js` 和 `history.js` 的用户输入渲染改为安全 DOM API / 文本赋值。
- icon 使用严格允许列表；未知历史值显示回退图标并保留原始存储值，用户主动选择后才写入新值。
- `input-safety.mjs`：97 passed / 0 failed。
- 反向验证同时覆盖注入类和允许列表类缺陷。
- CM-004、CM-003、CM-002 回归分别为 52/52、51/51、29/29；Lint 和空白检查通过。

## 当前门禁

主 AI 必须独立检查 `codex/cm005-input-safety` 的 diff、源码和测试；外部报告不自动等于 PASS。完整两轮报告和初审返工过程保留在 [`AI_HANDOFF_LEGACY_2026-09-18.md`](AI_HANDOFF_LEGACY_2026-09-18.md) 中，测试证据继续保留在 `tools/`。
