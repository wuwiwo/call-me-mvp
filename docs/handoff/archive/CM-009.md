# CM-009 归档 — 统一测试入口、CI（lint + 测试）与测试资产治理

> 本文件是 CM-009 的完整协作记录。当前状态见 `docs/AI_HANDOFF.md`。

## 任务卡摘要

- PHASE: Engineering / Tooling；PRIORITY: P2/P3
- 目标：单一稳定入口（一条命令串行跑 lint + 7 套件 + check-worktree）+ GitHub Actions CI + 测试资产治理（跨机器 Chrome 配置、端口分配、日志/证据规则）。
- SCOPE：`tools/run-all.mjs`、`tools/chrome-path.mjs`（新增）、`.github/workflows/ci.yml`（新增）、7 个套件的 Chrome 路径解析、`package.json`、`eslint.config.js`、`tools/README.md`。
- NON-GOALS：不改业务源码；不改断言语义；不做 format 基线；negative-* 不进 CI；不引入运行时依赖。

## 执行结果（外部 AI 报告要点）

提交（分支 codex/cm009-test-ci，基线 af83b9d）：

```text
6923c32  test: Chrome 路径解析统一到共享模块
303a9f2  test: 新增统一测试入口 tools/run-all.mjs
8b6ec85  chore: npm 脚本指向真实入口并登记 test 系列
0b280cf  ci: 新增 GitHub Actions 工作流（lint + 回归套件）
88c9be3  fix: eslint 忽略 .workbuddy/ 本地目录
c635814  docs: 登记统一入口、CI 与测试资产治理规则
557d6f8  docs: 回填 CM-009 执行报告与状态
```

- `node tools/run-all.mjs`：9/9 通过，477 项断言，228s；`--skip-browser` 快速路径 2/2。
- 7 套件断言数 29/51/52/97/87/107/54 与本任务前完全一致。
- CI 干跑：`npm ci --dry-run` / lint / test 全 0；YAML 结构断言 23/23。
- CHROME_PATH 抽验 e2e 29/0、button-ids 52/0。
- 过程中发现并修掉两处：`.workbuddy/` 打红 lint（eslint ignores 补齐）；`run-all.mjs` 的 `CM009_LOG` 空日志缺陷（输出全部改走 `out()`，31 处）。

## 主 AI 独立验收（2026-09-19）：PASS

| 验证项 | 命令 | 结果 |
|---|---|---|
| 改动范围 | `git diff --stat main..HEAD` | 14 文件全在 SCOPE；`js/`、HTML、CSS 零改动 |
| 空白检查 | `git diff --check` | 0 |
| 统一入口全量 | `CM009_LOG=... node tools/run-all.mjs` | 9/9 通过，477 断言，212s，exit 0 |
| 快速路径 | `node tools/run-all.mjs --skip-browser` | 2/2，2.7s，exit 0 |
| 日志落盘 | CM009_LOG 文件检查 | 6745 字节、含总览（`out()` 修复有效） |
| CI YAML 静态校验 | PyYAML 真实解析 + 24 项结构断言 | 24/24（触发器/权限/6 步/actions 版本/Node 22/npm ci/lint/test/artifact/无 negative run 步骤） |
| CHROME_PATH 抽验 | `CHROME_PATH=<chrome> node tools/cooldown.mjs` | 87/0，环境头显示覆盖值 |
| 路径解析优先级 | 7 项断言（套件覆盖 > CHROME_PATH > 默认；空白剔除；非法回退） | 7/7 |
| 完整性 | `node tools/check-worktree.mjs` | 缺失 0 |

执行 AI 报告措辞小瑕疵（不影响代码）：报告称 workflow「不含任何 negative-* 引用」，实际注释中含该字样（解释为何不跑）；run 步骤确实无引用。已在主 AI 校验脚本中按 run 步骤口径复核。

**流程合规**：本次合并前 main 未被修改（reflog 无提前 merge），协议顺序正确。

## 合并时级联事故（第三次实战复发，已恢复）

`git checkout main` 瞬间 `tools/` 再次被沙箱级联搬走：post-checkout 钩子报
`MODULE_NOT_FOUND tools/worktree-guard.mjs`（守卫与被保护物同归于尽，同 CM-007/008 根因），
`git status` 显示 11 条 ` D`。按既定程序恢复：

```text
git status          # 11 条 D，无非 D 条目 → 满足 restore 前提
git restore -- .    # 只写不删
check-worktree      # 缺失 0
```

随后 `git merge --ff-only codex/cm009-test-ci` 成功（`af83b9d..557d6f8`），合并后 porcelain 0 条目、
check-worktree 缺失 0、`run-all --skip-browser` 2/2。**三轮事故同一结论：防线脚本入库不能解决
「守卫住在 tools/ 里」的盲区；候选修法（守卫移出工作树 + 钩子内联应急回退）仍未实施。**

## 合并后状态

- main = 557d6f8（FF 合并）+ 主 AI 文档收尾提交。
- 主 AI 顺带同步 `AGENTS.md` 质量命令段：`npm test` / `test:quick` / CI 已存在，`TESTING.md` 定位为手工清单。
- ⚠️ **CI 未在真实 Actions 跑过**（本机无法验证）：首次 push 后必须人工核对 Actions 页面。

## 范围外发现（未修，留作后续）

1. `format` / `format:check` 仍走 prettier `.bin` shim（本机必失败）—— 属 format 基线任务。
2. `tools/worktree-guard.mjs:117` lint warning（`catch (e)` 未使用）—— 既有项。
3. CDP 9446 被 CM-004/006 共用 —— 已在端口分配表登记为已知项。
4. 防线盲区修法（守卫移出工作树）—— 等授权。
